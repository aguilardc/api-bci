spring:
  datasource:
    url: jdbc:h2:mem:users;NON_KEYWORDS=user
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always
  h2:
    console:
      enabled: true